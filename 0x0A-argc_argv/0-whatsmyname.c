#include <stdio.h>

/**
 * main - entrypoint
 *
 * @argc: arguments count
 * @argv: arguments vector
 * Return: Success
 */

int main(__attribute__((unused)) int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
