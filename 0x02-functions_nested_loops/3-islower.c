#include "main.h"
/**
 * _islower - checks for the case of an alphabet
 * c: argument to test
 * Return: lower(0) 0r upper(1)
 */

int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
